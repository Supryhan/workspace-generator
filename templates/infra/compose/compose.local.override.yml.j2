# compose.local.override.yml
#
# Local development override file for {{ workspace.display_name }}.
#
# Use this file to override service configurations during local debugging.
# This file is gitignored and regenerated by start-with-local-component.sh.
#
# Example: Scale down a service so your local IDE process handles its port.
#
# services:
#   students-service:
#     scale: 0
#
# Uncomment and customize as needed:

version: '3.8'

services: {}
